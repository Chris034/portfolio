# Christian Sarran — Portfolio

A responsive portfolio built with React, TypeScript, and Vite.

## Development

```bash
npm install
npm run dev
```

## Quality checks

```bash
npm run lint
npm run build
```

Preview the production build with `npm run preview`.
